# Field Archive

Field Archive keeps incident-site media clips, their review cases, and rendered delivery assets in a durable local store. Run `go run ./cmd/field-archive`; set `FIELD_ARCHIVE_DATA` to choose the persistence file.
