# Field Archive product brief

Emergency operations teams collect short video clips from field sites. Coordinators create review cases, register clips, and request a normalized delivery render. The service persists these records locally so work survives restart and exposes a small HTTP endpoint for a desktop companion. A cancelled request must remain visibly cancelled to the operator, rather than being reported as a rendering outage.
